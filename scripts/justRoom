node{
    state{
        scale=(-5,-5,-5);
    }
    resource=("textures/checkerboard.jpg","models/cube.obj");
}camera{
    position=(15, 15, 17);
    lookAt=(0,0,0);
    angularVelocity=(0,0,25);
    fov=90;
}
